extends layouts/main-layout.pug

block content
    .user-form
        p.users-title users table
        if users.length > 0
            ul.users-list
            each user in users
                li.users-list__item #{user}
        else 
            h4 No User Found